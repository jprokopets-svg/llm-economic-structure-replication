# Zero-Response Reanalysis

**Referee critique addressed:** exactly-zero forecast changes are scored as incorrect, which makes 50% the wrong chance benchmark when models frequently return unchanged forecasts.

The scoring rule at `scripts/score_fork.py:49` is

```python
def _sign_correct(td, md):
    return (td > 0 and md > 0) or (td < 0 and md < 0)
```

Both branches require a strict inequality on `md`, so `model_delta == 0` returns `False` and is counted as a miss. There is no tie branch and no exclusion. The repo already documents the consequence in another context: `outputs/analysis/simple_baselines_report.md:18-19` notes that the persistence baseline "always scores 0% because forecast_delta = 0 counts as incorrect by the scoring rule". A coin flip cannot produce a zero, but a model can, so a model that declines to move is penalised against a benchmark that never faces that choice.

Generated by `scripts/zero_response_reanalysis.py`. No existing scoring script or report was modified.

## Summary of what changes

**1. The central fork claim does not survive tie-adjusted scoring.** The published finding is that no model meets the preregistered success criterion and that three of four sit entirely below 50% (docs/MANUSCRIPT_NUMBERS.md:26). Scoring zeros as ties changes the characterisation for every model:

- **Claude Opus 4.8**: 30.6% (below chance) → 51.1% (indistinguishable from chance)
- **Claude Sonnet 4.6**: 37.6% (below chance) → 50.8% (indistinguishable from chance)
- **GPT-5.5**: 49.3% (below chance) → 57.2% (above chance)
- **Gemini 3.5 Flash**: 41.5% (below chance) → 55.0% (above chance)

No model is below chance once non-responses stop counting as wrong answers. Two (GPT-5.5, Gemini 3.5 Flash) move to *above* chance and also clear their own zero-preserving permutation null. This is a qualitative reversal of the headline claim, not a numerical adjustment.

**2. The zero rate is the mechanism, and it is large.** Between 15.9% and 40.8% of eligible pairs are exact non-responses. The ranking of models by published accuracy is almost exactly the inverse ranking by zero rate — Opus is 'worst' (30.6%) and has the most zeros (40.8%); GPT-5.5 is 'best' (49.3%) and has the fewest (15.9%). Conditional on actually moving, all four models cluster in a narrow 51-59% band. The published spread measures willingness to move more than directional skill.

**3. 50% was never the right benchmark, but the corrected benchmark is close to it.** The zero-preserving permutation null sits at 50.2-50.7% for all four models, so the referee is right that the benchmark was wrong in principle, but the size of the error is small. What moves the conclusion is the scoring of zeros in the numerator, not the location of the chance line.

**4. The Gemini temperature effect is an artefact of the same bug.** The published contrast (46.5% vs 60.3%) is significant (+13.8 pp, p = 0.0118), but the two arms are not scored on the same rule: temp-0 can return a zero and be marked wrong, while the temp-0.7 modal direction is forced to ±1 and never can. Scoring both consistently collapses the difference to +0.9 pp [-8.2, +10.4] (p = 0.8865) — indistinguishable from noise. The appendix claim 'conclusions not sensitive to deterministic decoding' happens to survive, but the number supporting it does not.

**5. The temperature slice covers half the cells its report claims.** A spend cap truncated the run: 26 cells contribute data, not 50. Any interval on that slice rests on 26 clusters.

**6. Stationarity exclusion moves the grid more than the fork.** Dropping all nine non-stationary configurations shifts fork accuracies by under 1 pp (no claim changes) but lowers every grid infer accuracy by 1.5-2.7 pp. The direction is consistent: the retained non-stationary cells were inflating grid performance. No grid conclusion reverses, but the numbers are not exclusion-invariant and the manuscript should say so.

**7. Two data-quality defects found in passing** (details in §3 and §4a): the stationarity report's table lists ten rows under a heading that admits nine, and the temperature report's accuracy numerator and denominator are accumulated over different pair sets.

---

## 1. Zero-response reanalysis (fork, ARM2−ARM1, eligible cells)

Eligible non-OOD variable×horizon pairs. Clustered bootstrap 10,000 draws, seed 42, cluster = world__param__setting (same cluster spec as the published primary).

- **(a) Zero share** — fraction with `model_delta == 0` exactly.
- **(b) Accuracy | nonzero** — published rule, restricted to pairs where the model actually moved.
- **(c) Tie-adjusted** — zeros score 0.5 instead of 0.
- **(d) Permutation null** — preserves each model's zero frequency; shuffles direction signs among nonzero responses only, within (world, param, horizon, seed); 10,000 permutations, seed 42.

| Model | Published acc | (a) Zero share [95% CI] | (b) Acc \| nonzero [95% CI] | (c) Tie-adjusted [95% CI] | (d) Null mean [2.5, 97.5] | n |
|-------|-------------:|------------------------:|---------------------------:|-------------------------:|-------------------------:|---:|
| Claude Opus 4.8 | 30.6% | 40.8% [38.2, 43.4] | 51.8% [49.3, 54.3] | 51.1% [49.6, 52.6] | 50.2% [49.4, 51.1] | 5893 |
| Claude Sonnet 4.6 | 37.6% | 26.5% [24.2, 28.9] | 51.2% [48.8, 53.4] | 50.8% [49.1, 52.5] | 50.2% [49.2, 51.2] | 5883 |
| GPT-5.5 | 49.3% | 15.9% [13.9, 17.8] | 58.6% [55.1, 62.0] | 57.2% [54.3, 60.1] | 50.7% [49.6, 51.8] | 5893 |
| Gemini 3.5 Flash | 41.5% | 27.0% [24.0, 29.9] | 56.8% [54.6, 59.1] | 55.0% [53.3, 56.7] | 50.3% [49.3, 51.3] | 5467 |

### 1e. Qualitative-claim changes

A model is 'below chance' only if its 95% CI upper bound is below the benchmark. Under the published rule the benchmark is 50%. Under tie-adjusted scoring the honest benchmark is the model's own permutation null, which is not 50% when the model returns zeros.

| Model | Published claim (vs 50%) | Tie-adjusted vs 50% | Tie-adjusted vs own null | Claim changes? |
|-------|--------------------------|---------------------|--------------------------|:--------------:|
| Claude Opus 4.8 | below chance | indistinguishable | indistinguishable | **YES** |
| Claude Sonnet 4.6 | below chance | indistinguishable | indistinguishable | **YES** |
| GPT-5.5 | below chance | above chance | above own null | **YES** |
| Gemini 3.5 Flash | below chance | above chance | above own null | **YES** |

## 2. Proportion not moving (response ratio exactly zero)

The manuscript states 'median response ratio 0.00' (docs/FORK_RESULTS_EXTRACT.md:116, docs/PAPER_SOURCE_OF_TRUTH.md:652). A median of exactly 0.00 is produced by a point mass at zero, not by a distribution centred near zero. The proportion below is the statistic the text should quote instead, because it says directly what the median only implies.

Response ratio = `delta_21 / true_delta`, on eligible non-OOD pairs with `|true_delta| > 1e-10` (the guard used at score_fork.py:282).

| Model | Proportion ratio exactly 0 [95% CI] | Median ratio | n |
|-------|------------------------------------:|-------------:|---:|
| Claude Opus 4.8 | 40.8% [38.2, 43.4] | 0.00 | 5893 |
| Claude Sonnet 4.6 | 26.5% [24.2, 28.9] | 0.00 | 5883 |
| GPT-5.5 | 15.9% [13.9, 17.8] | 0.00 | 5893 |
| Gemini 3.5 Flash | 27.0% [24.0, 29.9] | 0.00 | 5467 |

## 3. Gemini temperature-robustness uncertainty

Rebuilt from the same deterministic 10% cell subsample (50 cells, seed 42). Comparable pairs: **282** across **26** clusters (cluster = cell = world__param__setting__seed).

**Coverage flag — the published '50 cells' overstates the data.** The run carried a $5 spend cap (`scripts/gemini_temp_robustness.py:43`) and stopped after **160 of 300** scheduled calls, so only **27 of 50** sampled cells were ever called; 26 of those contribute eligible pairs. The published table row 'Cells in subsample | 50' reports the intended sample, not the achieved one — it is `len(sample)` (line 363), which is fixed before any API call. The 282 pairs are real, but they come from 26 cells, not 50, so the effective cluster count for any interval is roughly half what the report implies.

Both accuracies are computed over the SAME pair set. The published report divides `temp0_correct` by `temp07_n` (scripts/gemini_temp_robustness.py:367) although the numerator is accumulated at line 327, before the temp-0.7 availability guard at line 332 — so the published 46.5% can have a numerator and denominator drawn from different pair sets.

| Quantity | Published | Recomputed |
|----------|----------:|-----------:|
| Pairs compared | 282 | 282 |
| Temp-0 accuracy | 46.5% | 46.5% |
| Temp-0.7 modal accuracy | 60.3% | 60.3% |

| Statistic | Value |
|-----------|------:|
| Difference (temp-0.7 − temp-0) | +13.8 pp |
| Clustered bootstrap 95% CI (10,000 draws, seed 42) | [+4.6, +23.6] pp |
| Cluster sign-flip permutation p (10,000, seed 42) | 0.0118 |
| Clusters | 26 |

**One-line answer:** **Distinguishable from noise.** The 95% CI excludes zero (p = 0.0118).

### 3a. Zero-response confound in this comparison

Of the 282 temp-0 pairs, **73** (25.9%) have `temp0_delta == 0` and are scored incorrect automatically. The temp-0.7 arm cannot produce a zero at all: the modal direction is forced to ±1 (scripts/gemini_temp_robustness.py:341, where a tie maps to +1). The two arms are therefore not scored on the same rule, and part of the apparent temperature effect is the zero-scoring artefact rather than a decoding effect.

Scoring temp-0 zeros as ties (0.5) puts both arms on a comparable footing:

| Statistic | Value |
|-----------|------:|
| Temp-0 tie-adjusted accuracy | 59.4% |
| Difference (temp-0.7 − temp-0 tie-adjusted) | +0.9 pp |
| Clustered bootstrap 95% CI | [-8.2, +10.4] pp |
| Permutation p | 0.8865 |

## 4. Stationarity robustness

### 4a. The nine non-stationary configurations

| # | World | Param | Setting | max \|λ\| | Already excluded? |
|--:|-------|-------|--------:|--------:|:-----------------:|
| 1 | world1 | phillips_slope | 0.8 | 1.015 | no |
| 2 | world2 | phillips_slope | 0.8 | 1.063 | no |
| 3 | world2 | taylor_phi_pi | 2.5 | 1.021 | no |
| 4 | world3 | phillips_slope | 0.7 | 1.053 | **yes** |
| 5 | world3 | phillips_slope | 0.9 | 1.180 | **yes** |
| 6 | world4 | phillips_slope | 0.5 | 1.036 | no |
| 7 | world4 | taylor_phi_pi | 2.0 | 1.001 | no |
| 8 | world4 | taylor_phi_pi | 2.5 | 1.039 | no |
| 9 | world4 | wage_gap_slope | 1.0 | 1.036 | no |

**Data-quality flag (since fixed).** outputs/analysis/stationarity_report.md previously listed **ten** rows under the heading 'Non-stationary settings (max |λ| ≥ 1.0)', but `world2 / phillips_slope / 0.6` has max |λ| = 0.991, which is below 1.0. It is near-unit-root, not non-stationary. Dropping it leaves exactly the nine that the report's own summary table counts, so the count was right and the table had one row too many. The manuscript restatement (docs/MANUSCRIPT_NUMBERS.md:201-213) omits the row and was already internally consistent. The row has been moved out of that table into a note; the nine listed above are the non-stationary set.

**Already excluded:** the two World 3 Phillips cells (0.7, 0.9) are excluded from all published analyses via `EXCLUDED_GT_KEYS` (scripts/run_analysis.py:41) and were dropped at fork generation (scripts/run_fork.py:151-154).

### 4b. Do the other seven enter any analysis?

| World | Param | Setting | Fork eligible pairs | Grid infer rows |
|-------|-------|--------:|--------------------:|----------------:|
| world1 | phillips_slope | 0.8 | 500 | 388 |
| world2 | phillips_slope | 0.8 | 392 | 324 |
| world2 | taylor_phi_pi | 2.5 | 352 | 300 |
| world4 | phillips_slope | 0.5 | 548 | 504 |
| world4 | taylor_phi_pi | 2.0 | 480 | 414 |
| world4 | taylor_phi_pi | 2.5 | 492 | 378 |
| world4 | wage_gap_slope | 1.0 | 588 | 162 |

**7 of the 7** retained non-stationary configurations contribute rows to at least one headline analysis. They are not screened out anywhere.

### 4c. Fork ARM2−ARM1 excluding all nine non-stationary configs

| Model | Published | Recomputed (all) | Excl. 9 non-stationary | Δ vs published | Δ vs recomputed | n (excl.) |
|-------|----------:|-----------------:|-----------------------:|---------------:|----------------:|----------:|
| Claude Opus 4.8 | 30.6% | 30.6% | 30.5% [28.3, 32.9] | -0.1 pp | -0.1 pp | 5052 |
| Claude Sonnet 4.6 | 37.6% | 37.6% | 37.6% [35.3, 40.0] | -0.0 pp | -0.0 pp | 5042 |
| GPT-5.5 | 49.3% | 49.3% | 49.8% [46.5, 53.2] | +0.5 pp | +0.5 pp | 5052 |
| Gemini 3.5 Flash | 41.5% | 41.5% | 40.6% [37.9, 43.6] | -0.9 pp | -0.9 pp | 4638 |

### 4d. Grid infer all-tier excluding all nine non-stationary configs

Scored against a reconstructed per-seed ground truth. The file the published numbers used (`/tmp/lmm2_per_seed_test/ground_truth_per_seed.json`) no longer exists and no script in the repo regenerates it, so `scripts/rebuild_per_seed_gt.py` rebuilds it using the construction in `scripts/run_analysis.py::load_ground_truth` with the history seed varied over 0-9. The 'Recomputed (all)' column is the validation check: it should land on the published number if the reconstruction is faithful.

| Model | Published | Recomputed (all) | Excl. 9 non-stationary | Δ vs published | Δ vs recomputed | n (all) | n (excl.) |
|-------|----------:|-----------------:|-----------------------:|---------------:|----------------:|-------:|----------:|
| Claude Sonnet 4.6 | 54.9% | 54.9% | 53.4% [51.1, 55.7] | -1.5 pp | -1.5 pp | 6757 | 5828 |
| DeepSeek V4 Pro | 53.9% | 53.9% | 51.3% [46.8, 56.2] | -2.6 pp | -2.6 pp | 1387 | 1159 |
| GPT-5.5 | 73.1% | 73.1% | 71.0% [67.5, 74.5] | -2.1 pp | -2.1 pp | 6043 | 5386 |
| Gemini 3.5 Flash | 64.1% | 64.1% | 61.4% [57.1, 65.4] | -2.7 pp | -2.7 pp | 4991 | 4335 |

**Reconstruction validated.** Largest gap between the recomputed all-config accuracy and the published number is 0.0 pp, so the rebuilt per-seed ground truth reproduces the published pipeline and the exclusion deltas above are trustworthy.
